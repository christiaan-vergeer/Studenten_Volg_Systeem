@model IEnumerable<Studenten_Volg_Systeem.ViewModels.StudentListItemViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Studenten</h1>

<p>
    <a asp-action="Create">Maak Nieuwe Student Aan</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Fullname)*@
                Naam
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Course)*@
                Opleiding
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Student.Lastname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student.Adress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student.Adress)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fullname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Student.Lastname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Adress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Courses.FirstOrDefault(i => i.CourseId == item.Student.Course.CourseId).Name)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Pas Aan</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Verwijder</a>
            </td>
        </tr>
}
    </tbody>
</table>
